apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopay-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "shopay-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopay-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
