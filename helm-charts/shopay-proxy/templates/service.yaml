apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopay-proxy.fullname" . }}
  labels:
    {{- include "shopay-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopay-proxy.selectorLabels" . | nindent 4 }}
